export const runtimeConfig = {
  appId: 'wxc071e07c585e25bd',
  cloudEnabled: false,
  storageMode: 'local' as const,
  leaderboardMode: 'mock' as const,
  threatIntervalSeconds: { normal: [30, 45] as const, endless: [25, 35] as const }
};
